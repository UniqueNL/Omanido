
<h1>Featured items</h1>
<% @products.each do |product| %>
    <% if product.is_featured? %>
    <div class="featured">
      <div class="product-box-image no-float"><img class= "product-thumb"  src="<%=product.image%>" /></div>
      <%= link_to product.name, product_path(product.id)%><br />
      € <%= product.price %><br />
      <%= link_to "Show detailed information", product_path( product.id ), :class => "btn btn-primary btn-sm" %>
            </div>
    <% end %>
<% end %>

<h1 class="">Categories</h1>
<% @categories.each do |category| %>
 <div class="featured">
   <div class="product-box-image no-float">
     <img class= "product-thumb"  src="<%=category.image%>" />
     <%= link_to category.name, category_path(category.id), :class => "btn btn-primary btn-sm"%><br />
   </div>
 </div>
<% end %>

<link rel="stylesheet" type="text/css" href="css/fireworks.css" media="screen" />
<script type="text/javascript" src="js/fireworks.js"></script>

<div id="fireworks-template">
    <div id="fw" class="firework"></div>
    <div id="fp" class="fireworkParticle"><img src="images/particles.gif" alt="" /></div>
  </div>
<div id="fireContainer"></div>

<script>
var c=0;
var t;
var poping=0;

function displayFireworks() {
	createFirework(25,187,7,1,null,null,null,null,false,true);
	t=setTimeout(function(){displayFireworks()},2000);
}

function doTimer() {
if (!poping) {
  poping=1;
  displayFireworks();
  }
}

function stopCount() {
clearTimeout(t);
poping=0;
}

$( window ).load(function() {
  displayFireworks();
});
</script>
